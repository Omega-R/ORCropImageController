import XCTest
@testable import ORCropImageController

final class ORCropImageControllerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ORCropImageController().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
